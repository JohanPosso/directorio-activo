# Configuración de entorno (no usar credenciales reales aquí)

# Prisma / SQL Server
# Ejemplo de cadena (ajusta usuario, password, host, puerto y base):
# sqlserver://USER:PASS@HOST:1433;database=DB_NAME;encrypt=false;trustServerCertificate=true
DATABASE_URL="sqlserver://USER:PASS@HOST:1433;database=DB_NAME;encrypt=false;trustServerCertificate=true"

# Puerto HTTP del API
PORT=3000
